# Reservierungen-PWA (iPad) – v2

Neu:
- Heute-Liste zeigt Tischnummer + Bereich
- Neuer Screen: Tischplan (Tabs: Restaurant / Pizzastube / Terrasse)

## Lokal starten
```bash
npm install
cp .env.example .env
npm run dev
```

## Vercel
Scripts laufen ohne .bin-Exec-Flags (vite via node), um Exit 126 zu vermeiden.
